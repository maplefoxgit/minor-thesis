# Thesis Evidence Table

E1 to E4, E6, and E7 provide functional or security evidence. E5, E6-P, and S1 characterise local performance or scale and do not widen the functional claim.

| RQ | Artefact | Command | Expected evidence | Result file | Thesis limitation |
| --- | --- | --- | --- | --- | --- |
| RQ1 | Schema and semantic validation | `python experiments/run_e1_schema_expressiveness.py` | Valid baseline intent passes; bounded invalid intents fail for the expected reasons | The individual command prints JSON to standard output; the main E1 to E7 command retains E1 inside `results/reports/experiment_summary.json` and `results/reports/experiment_summary.md` | Limited to the fixed two-slice local intent model; `make validate` checks only the valid baseline intent and topology |
| RQ2 | Deterministic compiler outputs | `python experiments/run_e2_compiler_coherence.py` | Exactly three generated policy artefacts; coherent identifiers; stable repeated hashes | Generated policy bundle plus JSON on standard output; the main E1 to E7 command retains E2 inside `results/reports/experiment_summary.json` and `results/reports/experiment_summary.md` | Artefacts are representational, not production enforcement; `make compile` creates one bundle but does not perform the repeated E2 coherence study |
| RQ3 | Static reachability verification | `make verify` or `python experiments/run_e3_reachability_verification.py` | Both slice workloads can reach `shared_auth_log`; cross-slice workload and transport paths remain blocked; `shared_auth_log` is terminal | `results/reports/verification_report.json`, `results/reports/verification_report.md` | Proves only model-based static non-reachability |
| RQ3 | Negative-control rejection | `python experiments/run_e4_negative_controls.py` | Five unsafe topologies are rejected during validation; one over-restrictive topology is rejected during verification because both required shared-service paths are missing | The individual command prints JSON to standard output; the main E1 to E7 command retains E4 inside `results/reports/experiment_summary.json` and `results/reports/experiment_summary.md` | Limited to the six fixed negative-control fixtures |
| RQ1-RQ3 | Practical overhead evidence | `python experiments/run_e5_overhead.py` | Local stage timing, processor time, Python `tracemalloc` peak allocation, graph size, rule count, and file sizes are recorded | `results/metrics/overhead_metrics.json` | A single local run does not establish production scalability |
| RQ1-RQ3 | Repeated practical-overhead evidence | `python experiments/run_e5_repeated.py --warmups 5 --trials 30` | A retained 30-run distribution reports instrumented harness time, six separately measured stage timings, Python allocation, functional invariants, and environment metadata | `results/metrics/overhead_repeated.json`, `results/metrics/overhead_repeated.csv` | Characterises one host and one bounded graph; harness time is not pure pipeline time, and Python allocation is not whole-process memory |
| RQ3 | Controlled baseline comparison | `python experiments/run_e6_controlled_baselines.py` | Permissive topology-only preserves required access but blocks no forbidden paths; deny-all blocks forbidden paths but removes required access; the proposed compiled policy alone satisfies both objectives and keeps `shared_auth_log` terminal | `results/reports/baseline_comparison.json`, `results/reports/baseline_comparison.md` | Controlled synthetic comparison, not a production product benchmark |
| RQ3 performance characterisation | E6-P repeated verification timing | `python experiments/run_e6_repeated_performance.py --warmups 5 --trials 30 --iterations 500` | Five warm-up batches and 30 measured batches per condition report pure verification time for permissive, deny-all, and compiled-policy graphs, with 500 verifications per batch | `results/metrics/baseline_performance_repeated.json`, `results/metrics/baseline_performance_repeated.csv`, `results/reports/baseline_performance_repeated.md` | Timing is execution cost only, not functional or security evidence, a security-effectiveness score, or a comparison with another system |
| RQ2-RQ3 | Post-compilation policy integrity | `python experiments/run_e7_artifact_integrity.py` | The clean bundle verifies; a whitespace-only byte change leaves parsed policy meaning unchanged but changes its hash and is rejected before graph construction; clean regeneration restores the original hashes and passing result | `results/reports/artifact_integrity.json`, `results/reports/artifact_integrity.md` | Does not provide manifest authentication, detection of coordinated policy and manifest tampering, protection against a change after the check, topology, query, or report binding, detection of a compromised compiler or verifier, or runtime drift observation |
| RQ3 scale characterisation | S1 bounded verifier-only scale | `python experiments/run_s1_multislice_verifier_scaling.py --slices 2 3 4 10 --warmups 5 --trials 30 --rss-trials 5` | Generated graph cases representing 2, 3, 4, and 10 slices report verification time, median worker peak process resident memory, graph size, rule-equivalents, criteria, and path searches; the injected forbidden route is detected at every size | `results/metrics/multislice_verifier_scaling.json`, `results/metrics/multislice_verifier_scaling.csv`, `results/reports/multislice_verifier_scaling.md` | Bypasses the end-to-end intent schema, semantic validator, compiler, compiled-policy loader, and topology-to-policy graph builder; no capacity breaking point is claimed |
| RQ1-RQ3 | Main E1 to E7 experiment pack | `python experiments/run_all_experiments.py` | Seven experiment groups pass with no failures, and the summary report is thesis-readable | `results/reports/experiment_summary.json`, `results/reports/experiment_summary.md` | Does not run automated tests, repeated E5, E6-P, or S1; `make run-all` adds the automated tests but still does not run those three separate studies |
